---
version: "3"

tasks:
  archive:
    desc: Archive a file from the kubernetes folder
    cmds:
      - mkdir -p .archive/{{.DIRECTORY}}
      - mv {{.CLI_ARGS}} .archive/{{.CLI_ARGS}}
    vars:
      DIRECTORY:
        sh: dirname {{.CLI_ARGS}}
