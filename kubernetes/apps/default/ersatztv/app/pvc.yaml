---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ersatztv-config
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: replica-block
