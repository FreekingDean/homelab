---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: lidarr
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 0.0.14
      sourceRef:
        kind: HelmRepository
        name: freekingdean
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    defaultPodOptions:
      annotations:
        instrumentation.opentelemetry.io/inject-dotnet: monitoring/default
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
        fsGroupChangePolicy: "OnRootMismatch"
        supplementalGroups: [10000]
    stateful: true
    image:
      repository: lscr.io/linuxserver/lidarr
      tag: latest
    env:
      TZ: America/New_York
      PUID: 0
      PGID: 0
    ports:
      http: 8686
    ingress:
      enabled: false
    persistence:
      config:
        type: persistentVolumeClaim
        existingClaim: lidarr-config
      ec-media:
        type: persistentVolumeClaim
        existingClaim: ec-media
      media:
        type: persistentVolumeClaim
        existingClaim: replica-media
    serviceMonitor:
      enabled: false
