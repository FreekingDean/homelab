apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-sonarr-0
  namespace: default
  labels:
    app.kubernetes.io/instance: sonarr
    app.kubernetes.io/name: sonarr
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
  storageClassName: ceph-rbd
