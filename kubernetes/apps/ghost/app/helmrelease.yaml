---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ghost
  namespace: ghost
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 0.0.14
      sourceRef:
        kind: HelmRepository
        name: freekingdean
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      ghost:
        containers:
          ghost:
            envFrom:
              - secretRef:
                  name: ghost-secret
    stateful: true
    image:
      repository: docker.io/library/ghost
      tag: 6.0.5
    env:
      database__client: mysql
      database__connection__host: ghost-mariadb.ghost.svc.cluster.local
      database__connection__user: ghost
      database__connection__database: ghost
      mail__transport: SMTP
      mail__from: ghost@deangalvin.dev
      mail__options__service: SMTP
      mail__options__host: smtp-relay.default.svc.cluster.local
      mail__options__port: "25"
      TZ: America/New_York
      url: "https://ghost.deangalvin.dev"
      admin__url: "https://ghost.deangalvin.dev"
    ports:
      http: 2368
    ingress:
      subdomain: ghost
      localonly: false
      icon: "mdi:television-classic"
    persistence:
      config:
        type: persistentVolumeClaim
        existingClaim: ghost-config
        globalMounts:
          - path: /var/lib/ghost/content
    serviceMonitor:
      enabled: false
