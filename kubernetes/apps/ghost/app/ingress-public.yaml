apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app.kubernetes.io/instance: ghost
    app.kubernetes.io/name: ghost
  name: ghost-public
  namespace: ghost
spec:
  ingressClassName: traefik
  rules:
  - host: www.glimmerlabs.io
    http:
      paths:
      - backend:
          service:
            name: ghost
            port:
              number: 2368
        path: /
        pathType: Prefix
  - host: glimmerlabs.io
    http:
      paths:
      - backend:
          service:
            name: ghost
            port:
              number: 2368
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - ghost.deangalvin.dev
    secretName: deangalvin-dev-production-tls
status:
  loadBalancer:
    ingress:
    - ip: 10.1.21.2
