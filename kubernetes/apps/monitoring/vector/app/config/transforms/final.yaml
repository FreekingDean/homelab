type: remap
inputs:
  - "*_remapped"
  - "*_router._unmatched"
source: |
  tmp = del(.)

  .timestamp = tmp.timestamp
  .message = tmp.message
  .level = tmp.level || "debug"

  .data = tmp.data || {}
