---
apiVersion: v1
kind: Service
metadata:
  name: legacy-server
  namespace: networking
spec:
  type: ExternalName
  ports:
    - name: traefik-old
      port: 443
      targetPort: 443
  externalName: 10.0.0.131
  selector:
    app.kubernetes.io/instance: traefik
    app.kubernetes.io/name: traefik
