image:
  repository: busybox
  tag: latest

command: []

ports:
  http: 8080

ingress:
  enabled: true
  host: {{ .Release.Name }}.deangalvin.dev

serviceMonitor:
  port: http
  path: /metrics
  #interval: 1m
  #scrapeTimeout: 10s

persistence:
  secrets:
    config:
      name: myapp-secret
      items:
        - server.toml
